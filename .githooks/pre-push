#!/bin/sh
remote="${1:-origin}"
if command -v python3 >/dev/null 2>&1; then
    exec python3 -m tooling.git_guard pre-push --remote "$remote"
fi
exec python -m tooling.git_guard pre-push --remote "$remote"
